# low-power-fulladder
The given program implements a low-power 1-bit full adder using modular XOR–XNOR logic. The XOR–XNOR module generates both XOR and XNOR outputs simultaneously, which are reused in the full adder to compute sum and carry with reduced switching activity. 
